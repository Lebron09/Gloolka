<% fields = [:id, :first_name, :last_name, :address, :city, :manager_id] %>
<%= fields.map(&:to_s).join(";") %>
<% @developers.each do |developer| %>
<%= fields.map { |field| developer[field].to_s }.join(";") %>
<% end %>
