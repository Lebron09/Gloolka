<% fields = [:id, :first_name, :last_name, :address, :city] %>
<%= fields.map(&:to_s).join(";") %>
<% @managers.each do |manager| %>
<%= fields.map { |field| manager[field].to_s }.join(";") %>
<% end %>
